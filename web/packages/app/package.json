{
  "name": "@paraclete/app",
  "version": "0.1.0",
  "private": true,
  "description": "Theoria tablet client UI — Preact + canvas views (grid, encoder row, transport bar).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@paraclete/core": "*",
    "preact": "^10.23.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
