// SPDX-License-Identifier: GPL-3.0-or-later
//! Paraclete Snare Machine — CLAP instrument (AnalogEngine::snare + Sequencer).
//!
//! This binary becomes `paraclete_snare.clap` when compiled as a cdylib.
//! The CLAP FFI glue is added when clap-sys is introduced.

fn main() {
    // Placeholder. The .clap binary is a cdylib — this main() is never called.
}
